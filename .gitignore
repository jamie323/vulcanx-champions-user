# Local-only Claude Code scratch (worktrees, session metadata)
.claude/

# OS / editor cruft
.DS_Store
*.swp

# Bloodline art generation outputs — too large for git (1.6 GB+).
# Ground truth is the per-stage PNG in evolution20/<species>/<bloodline>/.
# allbloodlines/ is the staging dir from phase22 batch runs.
allbloodlines/
allbloodlines_v1/

# Browser-automation cache from local Playwright/Chrome MCP runs.
.playwright-mcp/

# Old arena prototype screenshots (pre-Campaign era).
arena-*.png

# Farhan NFT art bundles (large; art is already served from evolution20/)
Farhan_baby_mint_images/
Farhan_baby_mint_images.zip
Farhan_evolution_art_440/
Farhan_evolution_art_440.zip

# Superseded NFT spec drafts + scratch (nft_minting_spec_final.json is canonical)
all_nft_minting_spec.json
baby_nft_minting_spec.json
generate_full_nft_spec.js
bloodlines.json.bak
.claude/
adventures/_raw/
nft_images/icons/_orig_backup/
